#include "main.h"

/**
 * print_alphabet_*10 - prints 10 times the alphabet in lower case
 * followed by a new line
 */
int main(void)
{
	char ch;
	int i;

	i = 0
		while (i < 10)
		{
			ch = 'a'
			while (ch <= 'z')
			{
				_putchar(ch);
				ch++;
			}
			_putchar('\n');
			i++;
		}
}
