#include "main.h"
/**
 * main - print all alphabet in lowercase
 *
 * Return: always 0
 */
int main(void)

	char a, z;

	for (letter = 'a' ; letter <= 'z' ; letter)
	_putchar(letter);
	_putchar('\n');
	return (0);
}
